<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p>Priority: <%= task.priority %></p>
  <%= link_to "Delete?", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>